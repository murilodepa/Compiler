{Teste 1 - OK}
programa test1;

var a,b,c,x,y,h: inteiro;
j:booleano;

{proc 1}
procedimento analisa1;
var a, x,z: inteiro;
inicio
  se x  entao
      x:= 0;
  leia(z);
  enquanto (a != 89) e (a<= 1000)  {condicao de parada}
  faca inicio
           a:= x+z
       fim;
  escreva(a)
fim;


procedimento analisa2;
inicio
  leia(a);
  j:= (x + 7 * 5  div (30+y) <= (x*a+2))e(h>0)
fim;

inicio
    analisa1;
    analisa2;
    se b > (c+ a*a)
    entao escreva(b)
    senao escreva(c)
fim.

{fim}